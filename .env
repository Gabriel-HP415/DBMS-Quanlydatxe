# Copy to .env before: docker compose up --build
# Password phải đủ mạnh: chữ hoa, thường, số, ký tự đặc biệt

MSSQL_SA_PASSWORD=BusBooking@2026
# Port host (tranh xung dot SQL Server / backend local)
SQLSERVER_PORT=14333
BACKEND_PORT=3002
FRONTEND_PORT=8080